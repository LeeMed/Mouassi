<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leemed.mouassi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/splashScreenTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".AddPictureActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".DownloadActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".UsActivity" />
        <activity android:name=".UseroActivity" />
        <activity android:name=".UsermActivity" />
        <activity android:name=".AboutUsoActivity" />
        <activity android:name=".AboutUsaActivity" />




        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>